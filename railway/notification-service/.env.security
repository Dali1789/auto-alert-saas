# SECURITY CONFIGURATION TEMPLATE
# Copy these variables to your production environment

# CRITICAL: Generate strong webhook secret (minimum 32 characters)
WEBHOOK_SECRET=your-super-secure-webhook-secret-minimum-32-characters-long

# TEST API KEY (for development/testing endpoints)
TEST_API_KEY=generate-random-32-char-key-for-testing

# SERVICE API KEY (for internal service communication)
SERVICE_API_KEY=another-secure-32-char-key-for-services

# RATE LIMITING CONFIGURATION
RATE_LIMIT_MAX=1000
RATE_LIMIT_WINDOW=900000

# SECURITY FEATURE TOGGLES
ENABLE_CSRF_PROTECTION=true
ENABLE_REQUEST_LOGGING=true
ENABLE_SECURITY_HEADERS=true

# MONITORING AND ALERTING
SENTRY_DSN=your-sentry-dsn-for-error-tracking
LOG_LEVEL=info

# INSTRUCTIONS:
# 1. Generate secure random keys using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 2. Never commit actual keys to version control
# 3. Set these in Railway environment variables
# 4. Test security with: curl -H "X-API-Key: wrong-key" https://your-app/api/webhooks/test